import { AttdirectiveDirective } from './attdirective.directive';

describe('AttdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AttdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
